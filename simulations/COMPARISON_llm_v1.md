# LLM Level-1 vs Heuristic Baseline (first 10 seeds)

## Overview

- **Heuristic batch:** `simulations/batch_v0.7.3.jsonl` (first **10** matches), `rules_version` **v0.7.3**.
- **LLM batch:** `simulations/batch_llm_v1.jsonl` (same seeds / tribes / turn orders / schedules as heuristic row, agents swapped to `*_llm`).
- **Validation:** `python3 tools/sim.py --validate simulations/batch_llm_v1.jsonl` should exit **0** after generation.
- **Model / plumbing:** `glm-4.6` (see `run_metadata.runner_model` in each LLM match).
- **Trace sidecar:** `simulations/llm_trace_batch_llm_v1.jsonl` (one row per LLM seat).
- **API key at report time:** present — if absent, the 10-match run used **MockLLMClient** (deterministic stub policy; no external spend).
- **Rough API cost estimate (from trace token fields):** **$0.7981** USD (provider `zai`).

## End-trigger distribution (paired seeds)

| trigger | heuristic (n=10) | LLM (n=10) |
|---|---:|---:|
| `round_limit` | 10 | 7 |
| `vp_threshold` | 0 | 3 |

## Match length (`final_round`)

- **Avg:** heuristic **15.00** vs LLM **15.00**
- **Histogram heuristic:** {15: 10}
- **Histogram LLM:** {15: 10}

## Trade activity

- **Avg completed trades / match:** heuristic **3.50** vs LLM **0.10**
- **Avg `trade_rejected` events / match:** heuristic **7.50** vs LLM **1.00**
- **Avg bead conversion events / match:** heuristic **2.30** vs LLM **0.00**

## Ambush activity

- **Avg ambushes attempted / match:** heuristic **2.40** vs LLM **0.00**

## Winner VP distribution

- **Avg winning VP:** heuristic **7.00** vs LLM **6.20**

## Archetype win rates (paired seeds)

> Note: the first ten slots in `batch_v0.6_initial_config.json` are **mirror greedy_builder ×4** schedules; both batches therefore mostly attribute wins to **`greedy_builder`** unless schedules differ.

| archetype | heuristic wins | LLM wins |
|---|---:|---:|
| `greedy_builder` | 10 | 10 |

## LLM-specific metrics (trace sidecar)

- **Avg retries per LLM seat:** **0.136**
- **Fallback rate (% of LLM seats):** **2.35%**
- **Avg latency / seat:** **5667.0 ms**
- **Avg tokens / seat:** **1719** (in ~1528, out ~191)
- **Total LLM seats traced:** **597**

## Qualitative observations

- Review `simulations/TRANSCRIPTS_llm_v1.md` after real runs — below are placeholders until refreshed.
- **Heuristic-only behaviors:** compare trace reasoning vs greedy_builder policy in seeds 1–10.
- **Alliances:** watch `banker_llm` / `alliance_duopoly_llm` schedules on later batches.

- **Tribute Route usage (proxy: `tribute_route_payment` substring hits in LLM JSON):** **0** flags across 10 matches.

## Conclusions

- **Level 2 NL negotiation:** Defer until a **keyed** LLM batch shows richer trade discourse than structured JSON; mock runs validate wiring only.

---

*Auto-generated by `tools/compile_llm_comparison.py`. Regenerate after new batch/trace runs.*
