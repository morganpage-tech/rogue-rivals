# LLM Level-1 vs Heuristic Baseline (first 10 seeds)

## Overview

- **Heuristic batch:** `simulations/batch_v0.7.3_archetype_slice.jsonl` (first **20** matches), `rules_version` **v0.7.3**.
- **LLM batch:** `simulations/batch_llm_v2.jsonl` (same seeds / tribes / turn orders / schedules as heuristic row, agents swapped to `*_llm`).
- **Validation:** `python3 tools/sim.py --validate simulations/batch_llm_v2.jsonl` should exit **0** after generation.
- **Model / plumbing:** `glm-4.6` (see `run_metadata.runner_model` in each LLM match).
- **Trace sidecar:** `simulations/llm_trace_batch_llm_v2.jsonl` (one row per LLM seat).
- **API key at report time:** present — if absent, the 10-match run used **MockLLMClient** (deterministic stub policy; no external spend).
- **Rough API cost estimate (from trace token fields):** **$1.4579** USD (provider `zai`).

## End-trigger distribution (paired seeds)

| trigger | heuristic (n=10) | LLM (n=10) |
|---|---:|---:|
| `great_hall` | 7 | 0 |
| `round_limit` | 0 | 8 |
| `vp_threshold` | 13 | 12 |

## Match length (`final_round`)

- **Avg:** heuristic **8.40** vs LLM **14.70**
- **Histogram heuristic:** {8: 3, 9: 9, 10: 1, 7: 6, 11: 1}
- **Histogram LLM:** {15: 16, 14: 3, 12: 1}

## Trade activity

- **Avg completed trades / match:** heuristic **10.95** vs LLM **8.90**
- **Avg `trade_rejected` events / match:** heuristic **14.05** vs LLM **8.00**
- **Avg bead conversion events / match:** heuristic **9.80** vs LLM **8.05**

## Ambush activity

- **Avg ambushes attempted / match:** heuristic **1.25** vs LLM **0.95**

## Winner VP distribution

- **Avg winning VP:** heuristic **8.00** vs LLM **7.70**

## Archetype win rates (paired seeds)

> Note: the first ten slots in `batch_v0.6_initial_config.json` are **mirror greedy_builder ×4** schedules; both batches therefore mostly attribute wins to **`greedy_builder`** unless schedules differ.

| archetype | heuristic wins | LLM wins |
|---|---:|---:|
| `aggressive_raider` | 0 | 5 |
| `banker` | 7 | 6 |
| `diversified_trader` | 12 | 3 |
| `greedy_builder` | 1 | 6 |

## LLM-specific metrics (trace sidecar)

- **Avg retries per LLM seat:** **0.439**
- **Fallback rate (% of LLM seats):** **10.65%**
- **Avg latency / seat:** **8068.1 ms**
- **Avg tokens / seat:** **1597** (in ~1407, out ~190)
- **Total LLM seats traced:** **1155**

## Qualitative observations

- Review `simulations/TRANSCRIPTS_llm_v1.md` after real runs — below are placeholders until refreshed.
- **Heuristic-only behaviors:** compare trace reasoning vs greedy_builder policy in seeds 1–10.
- **Alliances:** watch `banker_llm` / `alliance_duopoly_llm` schedules on later batches.

- **Tribute Route usage (proxy: `tribute_route_payment` substring hits in LLM JSON):** **0** flags across 20 matches.

## Conclusions

- **Level 2 NL negotiation:** Defer until a **keyed** LLM batch shows richer trade discourse than structured JSON; mock runs validate wiring only.

---

*Auto-generated by `tools/compile_llm_comparison.py`. Regenerate after new batch/trace runs.*
